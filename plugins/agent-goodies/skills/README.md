# Shared skills

Place each portable skill in its own `<skill-name>/SKILL.md` directory. Keep
host-specific tool names, invocation controls, and substitutions out of shared
skill files unless both target hosts support them.
